<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div>
        <h1 class="mt-3">Welcome, <%= currentUser.name %>!</h1>
        <button type="submit" >Logout</button>
    </div>
    
   <form  class="mt-3" action="/post" method="post">
   <textarea class=" w-1/3 block resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3"
    placeholder="post.." name="content" ></textarea>
    <input class="px-3 py-1 bg-blue-500 block rounded-md mt-2" type="submit">
   </form>

   <h2 class="mt-20 ">your posts</h2>
   <div class="post-container mt-5">
    <% currentUser.posts.forEach(element => { %>
        <div class="post w-1/3 p-4 border-2 border-zinc-600 ">
            <h4>@<%=currentUser.username %></h4>
            <p><%= element.content %></p>
            <div class="btns flex gap-4">
               <a class="bg-blue-500 px-3 rounded-md" href="">Like</a>
               <a class="bg-zinc-500 px-3 rounded-md" href="">Edit</a>
            </div>
       </div>
    <% }) %>
   </div>

</body>
</html>